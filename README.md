# Flask dockerise

Planning to deploy this to K8s within GCP. Infrastructure definition to be held in Terraform

# Setup

Pull down from Git

Setup virtual env (do it in PyCharm)

Go to the requirements.txt and let PyCharm install the requirements. Otherwise do it through pip